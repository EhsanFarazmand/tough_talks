# ============================================================
# Tough Talks — Python Dependencies
# Grouped by phase. All versions pinned for reproducibility.
# ============================================================

# --- Phase 1: Model access & function calling ---
transformers>=4.40.0
accelerate>=0.27.0
bitsandbytes>=0.43.0        # 4-bit / 8-bit quantization
torch>=2.2.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# --- Phase 2: Audio & transcription ---
# Step 03 uses Gemma 4 E2B's native audio path — no separate ASR model.
# librosa + soundfile back the AutoProcessor's audio feature extractor.
librosa>=0.10.1             # Audio feature extraction
soundfile>=0.12.1           # Audio I/O
numpy>=1.26.0
scipy>=1.12.0
webrtcvad>=2.0.10           # Voice activity detection (live mode gating)

# --- Phase 3: Intelligence features ---
# (Pure Python + transformers — no extra deps)
scikit-learn>=1.4.0         # Pattern clustering for Talk DNA
nltk>=3.8.1                 # Linguistic analysis
spacy>=3.7.4                # NLP pipeline
textblob>=0.18.0            # Sentiment baseline

# --- Phase 4: Debrief & memory ---
tiktoken>=0.6.0             # Token counting for prompt management
difflib                     # stdlib — plan vs. reality diff (no install)

# --- Phase 5: Backend API ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-multipart>=0.0.9     # Audio file uploads
aiofiles>=23.2.1            # Async file I/O

# --- Phase 6: Frontend integration ---
httpx>=0.27.0               # Async HTTP client for testing
websockets>=12.0            # Real-time emotion radar stream

# --- Dev & testing ---
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
black>=24.3.0
ruff>=0.3.0
ipykernel>=6.29.0
jupyter>=1.0.0
notebook>=7.1.0

# --- Kaggle / Colab compatibility ---
# kaggle                    # Uncomment if running kaggle CLI
# google-colab              # Auto-available in Colab, not installable locally
